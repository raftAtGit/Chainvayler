/** Implementation classes. Regular users should not need to use them normally. 
 * Most of the classes and interfaces are public because they are used by injected bytecode. */
package raft.chainvayler.impl;

