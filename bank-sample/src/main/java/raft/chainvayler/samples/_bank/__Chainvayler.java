package raft.chainvayler.samples._bank;

import org.prevayler.Prevayler;

import raft.chainvayler.impl.Context;
import raft.chainvayler.impl.RootHolder;

/**
 * Normally this class is created by Chainvayler {@link Compiler} 
 * 
 * @author r a f t
 */
@_Injected
public class __Chainvayler extends Context {

	public __Chainvayler(Prevayler<RootHolder> prevayler, RootHolder root) {
		super(prevayler, root);
	}
}
